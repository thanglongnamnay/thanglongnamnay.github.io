const imagesList=document.getElementById("images"),observer=new IntersectionObserver(handleNewContentObserve,{threshold:.5});let prevY=0;async function handleNewContentObserve(e){if(e){if(!e[0].isIntersecting)return;observer.unobserve(e[0].target)}console.log("Loading new images"),await wait(1e3);const n=await getNewContents();for(el of n)imagesList.appendChild(el)}handleNewContentObserve();