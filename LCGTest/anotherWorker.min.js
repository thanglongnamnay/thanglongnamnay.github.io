"use strict";const MAX=1e9;let DISTRIBUTE_PART;function throttle(t,e){let n=Date.now()-e;return function(...o){Date.now()-n>=e&&(t.apply(this,o),n=Date.now())}}function average(t){let e=0;const n=function(t){self.postMessage({average:e/t})},o=throttle(n,100);for(let n=1;n<=MAX;++n)e+=t(),o(n);return n(MAX),e/MAX}function distribution(t,e){const n=[],o=throttle(function(){self.postMessage({distribution:n})},100);for(let t=0;t<e;++t)n.push(0);for(let a=0;a<MAX;++a){const a=t()*e>>0;++n[a],o()}return n.map(t=>t/MAX)}self.onmessage=t=>{let e;switch(t.data.DISTRIBUTE_PART&&(DISTRIBUTE_PART=t.data.DISTRIBUTE_PART),t.data.type){case"Math.random":e=Math.random;break;default:e=Math.random}distribution(e,DISTRIBUTE_PART)};